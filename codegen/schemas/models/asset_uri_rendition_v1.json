{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "properties": {
    "uri": {
      "description": "The URI to use in the rendition URI or in the document rendition URI template.  May include a download ticket.",
      "format": "uri",
      "type": "string"
    }
  },
  "required": [
    "uri"
  ]
}