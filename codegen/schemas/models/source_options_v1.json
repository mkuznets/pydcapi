{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "additionalProperties": {
    "additionalProperties": {
      "properties": {
        "export_uri": {
          "description": "Export URI needed for asset export operation. The URI is specific to the destination cloud for assets from origin cloud. Export destination and import origin may be the same in most cases.",
          "type": "string"
        }
      },
      "required": [
        "export_uri"
      ],
      "type": "object"
    },
    "type": "object"
  },
  "type": "object"
}