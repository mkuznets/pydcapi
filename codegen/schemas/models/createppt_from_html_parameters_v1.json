{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "additionalProperties": false,
  "dependencies": {
    "parent_uri": {
      "properties": {
        "persistence": {
          "enum": [
            "permanent"
          ]
        }
      },
      "required": [
        "persistence"
      ]
    }
  },
  "properties": {
    "asset_uri": {
      "description": "Asset URI identifying the HTML file to convert to PowerPoint. Must be an ACP (Platform) asset.",
      "format": "uri",
      "type": "string"
    },
    "name": {
      "description": "Name of the output PPTX asset (e.g. presentation.pptx). Duplicate name behaviour is controlled by on_dup_name.",
      "minLength": 1,
      "type": "string"
    },
    "on_dup_name": {
      "default": "auto_rename",
      "description": "How to handle a duplicate name conflict for the output file.",
      "enum": [
        "error",
        "auto_rename",
        "overwrite"
      ],
      "type": "string"
    },
    "parent_uri": {
      "description": "The uri of folder to put the asset in.  This parameter is relevant only for permanent assets.  If not specified, the default depends on the operation.  Conversions will be placed in the same folder as the source asset.",
      "format": "uri",
      "type": "string"
    },
    "persistence": {
      "default": "transient",
      "description": "Asset storage aspect as short-term transient vs. long-term permanent. \"transient\" creates an asset that will be available for several hours before being garbage collected and deleted.  For operations that convert and download immediately, \"transient\" is the appropriate choice",
      "enum": [
        "transient",
        "permanent"
      ],
      "type": "string"
    }
  },
  "required": [
    "asset_uri",
    "name"
  ]
}